/**
 * Deprecated. Content moved to /practice (app/practice/page.tsx).
 *
 * This component used to render a long, sitewide practice-overview block
 * inside the footer on every page. Client feedback flagged it as repetitive,
 * so the content was moved to a dedicated /practice page (linked from the
 * footer's Quick Links). Keeping the file as a no-op export prevents any
 * stray imports from breaking the build.
 *
 * Safe to delete this file once you're sure no third-party tooling references it.
 */
export function FooterPracticeSummary() {
    return null;
}
